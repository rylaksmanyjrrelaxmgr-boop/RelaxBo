# ============================================================
# متطلبات البوت - إصدارات متوافقة ومستقرة
# تم اختبارها للعمل مع Python 3.14 على Render
# ============================================================

# --- أساسيات البوت ---
python-telegram-bot==20.7
httpx==0.25.2
httpcore==1.0.2
anyio==3.7.1
aiohttp==3.9.0
aiosqlite==0.19.0
python-dotenv==1.0.0
cachetools==5.3.3
psutil==5.9.6
nest-asyncio==1.6.0

# --- الأمان والتشفير ---
cryptography==41.0.7

# --- الترجمة والتنظيف ---
deep-translator==1.11.0
bleach==6.1.0

# --- أدوات متنوعة ---
qrcode==7.4.2
plotly==5.18.0
aiofiles==23.2.1
reportlab==4.0.9
jinja2==3.1.3
markdown==3.5.2
python-multipart==0.0.9
pyotp==2.9.0

# --- خدمات Google (اختياري، لكن مفيد) ---
google-auth==2.28.1
google-auth-oauthlib==1.1.0
google-api-python-client==2.111.0

# --- خادم الويب (للوحة التحكم) ---
fastapi==0.110.0
uvicorn==0.29.0
pydantic==2.5.3  # إصدار أكثر استقراراً من 2.6.4

# --- تبعيات أساسية أخرى ---
typing-extensions==4.10.0
attrs==23.2.0
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.6
urllib3==2.2.1
multidict==6.0.5
yarl==1.9.4
frozenlist==1.4.1
aiosignal==1.3.1
propcache==0.2.0

# --- تم تعطيل المكتبات غير المتوافقة مع Python 3.14 مؤقتاً ---
# Pillow==10.2.0  # غير متوافق
# opencv-python-headless==4.9.0.80  # غير متوافق
# zstandard==0.22.0  # غير متوافق

# ============================================================
# ملاحظات هامة:
# 1. تم تثبيت إصدار pydantic==2.5.3 لأنه أكثر استقراراً
# 2. تم تعطيل Pillow و opencv-python-headless و zstandard
# 3. البوت سيعمل بكامل ميزاته الأساسية بدون هذه المكتبات
# 4. لتحديث هذه المكتبات مستقبلاً، تحقق من توافقها مع Python 3.14
# ============================================================
